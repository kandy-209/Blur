# Minimal requirements for Railway deployment
# Core dashboard dependencies
dash>=2.14.0
dash-auth>=1.5.1
pandas>=2.0.0
plotly>=5.17.0
yfinance>=0.2.28
gunicorn>=21.2.0
numpy>=1.24.0
requests>=2.31.0
pytz>=2023.3

# Basic technical analysis (lighter alternative to ta-lib)
pandas-ta>=0.3.14b0

# Optional but lightweight
scikit-learn>=1.3.0
textblob>=0.17.1
vaderSentiment>=3.3.2
beautifulsoup4>=4.12.0
fredapi>=0.5.1

# Note: Heavy packages removed for faster builds:
# - ta-lib (requires system libraries, very slow)
# - xgboost (slow to compile)
# - sentence-transformers (downloads large models)
# - faiss-cpu (slow to build)
# - openai, anthropic (only needed if using LLM features)

